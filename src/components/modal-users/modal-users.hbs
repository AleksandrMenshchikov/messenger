<div>
  <div class="modal-users">
    <div class="modal-user__wrapper">
      {{{modal-users-title}}}
      <div class="search">
        {{{input-search}}}
        <img class="search__icon" src="{{search}}" alt="Иконка" />
        {{{button-search}}}
      </div>
      {{{list-users}}}
    </div>
  </div>
</div>
